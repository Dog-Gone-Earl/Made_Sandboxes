init_config:

    loader: core

    use_device_id_as_hostname: true
instances:
  -
    ip_address: localhost
    snmp_version: 3
    loader: core
    use_device_id_as_hostname: true
    user: snmpadmin
    authProtocol: SHA
    authKey: 'datadog2023'
    privProtocol: AES
    privKey: 'ddsnmp2023'
    tags:
      - minor:jammy